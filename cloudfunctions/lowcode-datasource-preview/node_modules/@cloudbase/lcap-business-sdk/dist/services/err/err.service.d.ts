import { LoggerService } from '../logger/logger.service';
export declare class ErrService {
    private readonly loggerService;
    constructor(loggerService: LoggerService);
    throw(code: number, msg: string): void;
}
