import { ILogInfo } from './logger.interface';
export declare class LoggerService {
    info(logInfo: ILogInfo): void;
    warn(logInfo: ILogInfo): void;
    error(logInfo: ILogInfo): void;
    private print;
}
