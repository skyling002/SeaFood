import { IHttpAuthProvider } from '../../types';
export declare function getAuthData(dsId: string): Promise<{
    authInfo: any;
    qs: {
        key: any;
    };
}>;
export declare const provider: IHttpAuthProvider;
