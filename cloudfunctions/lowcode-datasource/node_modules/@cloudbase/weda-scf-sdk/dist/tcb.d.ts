import cloudbase from '@cloudbase/node-sdk';
interface IConfig {
    env: any;
    /**
     * 微搭后台环境类型, 与 url 意义类似, 但可通过设置为 prod/pre 来指定地址, **优先使用 target**
     * 可以是下边三个值
     *  'prod' 正式地址
     *  'pre' 预发地址
     *  'demo' 体验地址
     *  url 任意一个url
     */
    wedaTarget?: string;
    /** 环境类型 */
    envType: 'pre' | 'prod';
    /** 环境信息 */
    contextEnv?: Record<string, any>;
}
export declare function init(cfg: Partial<IConfig>): void;
export declare function getConfig<K extends keyof IConfig>(k: K): IConfig[K];
/** 获取App */
export declare function getApp(): cloudbase.CloudBase;
export declare const tcb: typeof cloudbase;
interface ICloudbaseContext extends cloudbase.ICompleteCloudbaseContext {
    WEDA_UUID?: string;
    /** uid 类型, 1.tcb 2.微信 3.企业微信 4.weda内部 */
    WEDA_SOURCE?: 1 | 2 | 3 | 4;
}
/**
 * 解析云函数的 context
 *  为保证能获取到完整的context信息, 应当保证在第一次调用时有传入 context
 * @param context tcb 云函数的context, 即 main 函数的第二个参数
 */
export declare function getCloudbaseContext(): ICloudbaseContext;
export {};
