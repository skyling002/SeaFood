import request from 'request';
import { IPlatformDataSource, IHttpMethod } from '@cloudbase/cals';
import { IFnContext } from '../types';
export declare function isSuccessStatusCode(code: number): boolean;
export declare function sendRequest(ds: IPlatformDataSource, rawMethodConfig: IHttpMethod, context: IFnContext, params: object): Promise<{
    response: request.Response;
    body: unknown;
}>;
