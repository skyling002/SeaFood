import { ConfigService } from '../config/config.service';
import { ErrService } from '../err/err.service';
import { TcbService } from '../tcb/tcb.service';
/**
 * 腾讯云 云API
 */
export declare class TcloudRequestService {
    private readonly configService;
    private readonly errService;
    private readonly tcbService;
    private tcloudConfig;
    constructor(configService: ConfigService, errService: ErrService, tcbService: TcbService);
    /**
     * 请求腾讯云API v3（兼容本地开发和线上数据源）
     */
    requestV3<T = any>(params: {
        service: string;
        action: string;
        actionParams?: any;
    }): Promise<T>;
    /**
     * 获取数据源底层的数据库名
     * @param dsNameList 数据源标识列表
     * @returns
     */
    getDsColName(dsNameList: string[]): Promise<string[]>;
    /**
     * 获取云API请求的v3签名
     */
    private getHeadersV3;
    /**
     * 计算数据源缓存键名
     * @param dsName 数据源标识
     * @returns
     */
    private getDsCacheKey;
}
