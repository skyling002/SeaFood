import { ConfigService } from '../config/config.service';
import { TcbService } from '../tcb/tcb.service';
import { LoggerService } from '../logger/logger.service';
import { IRequestParams } from './weda-request.interface';
export declare class WedaRequestService {
    private readonly configService;
    private readonly tcbService;
    private readonly loggerService;
    constructor(configService: ConfigService, tcbService: TcbService, loggerService: LoggerService);
    /**
     * 请求微搭请求后端
     */
    request<T>(params: IRequestParams): Promise<T>;
    /**
     * 当前用户是否为管理员用户
     * doc: https://tcloud-dev.oa.com/document/product/1505/64326?!preview&!document=1
     */
    isAdminRole(opts?: {
        prevPass: boolean;
    }): Promise<boolean>;
    /**
     * 获取用户来源信息
     */
    private getUserSource;
}
