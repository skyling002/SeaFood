import { ILcDatasourceCtx } from '../context/context.interface';
/**
 * 加载读取配置（包括命令行变量和配置文件）
 */
export declare class ConfigService {
    private envParser;
    private lcDatasourceCtx;
    constructor(lcDatasourceCtx: ILcDatasourceCtx);
    /**
     * 获取命令行变量值。本地开发模式下，还会读取.env中声明的变量
     */
    getEnvironmentVariable(key?: string): any;
    /**
     * 判断是否是本地开发模式
     */
    isLocalDev(): boolean;
    /**
     * 获取微信小程序用户ID，支持共享小程序
     */
    get wxOpenId(): string;
    /**
     * 获取微信AppID，支持共享小程序
     */
    get wxAppId(): string;
    /**
     * 是否是数据源正式环境
     */
    get isDsOnline(): boolean;
    /**
     * 获取微搭环境的应用ID
     */
    get wedaAppId(): string;
}
